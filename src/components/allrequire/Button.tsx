import "../../styles/Button.css"

export const Button = () => {
    return(
        <div className="buttons">
            <button id="send">Send</button>
            <button>Add</button>
        </div>
    )
}